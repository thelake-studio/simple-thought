{% extends 'base.html.twig' %}

{% block title %}Registrar Progreso - Simple Thought{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-10 col-xl-8">

            {# --- Cabecera --- #}
            <header class="mb-4">
                <a href="{{ path('app_goal_show', {'id': goal.id}) }}" class="text-decoration-none text-secondary small mb-3 d-inline-block" aria-label="Volver al objetivo">
                    <i class="fa-solid fa-arrow-left me-2"></i>Volver al objetivo
                </a>
                <h1 class="h3 fw-bold text-primary mb-1">
                    <i class="fa-solid fa-plus-circle me-2"></i>Registrar Progreso Manual
                </h1>
                <p class="text-muted small mb-0">Añade un nuevo avance para: <strong class="text-dark">{{ goal.name }}</strong></p>
            </header>

            {# --- Formulario --- #}
            {{ include('goal_log/_form.html.twig', {
                'button_label': 'Guardar Registro',
                'goal': goal
            }) }}

        </div>
    </div>
</div>
{% endblock %}
