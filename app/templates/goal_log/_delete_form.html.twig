<form method="post" action="{{ path('app_goal_log_delete', {'id': log_id}) }}" onsubmit="return confirm('¿Estás seguro de que quieres borrar este registro del historial?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ log_id) }}">
    <button class="btn btn-outline-danger w-100">
        <i class="bi bi-trash"></i> Eliminar Registro
    </button>
</form>
