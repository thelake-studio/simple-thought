{% extends 'base.html.twig' %}

{% block title %}Inicio - Simple Thought{% endblock %}

{% block body %}
<div class="container-fluid py-2">

    {# --- Cabecera de Bienvenida --- #}
    <header class="mb-5">
        <h1 class="display-5 fw-bold text-primary">Hola, {{ app.user.nickname }} 👋</h1>
        <p class="lead text-muted mb-0">Aquí tienes el control de tu bienestar emocional.</p>
    </header>

    {# --- SECCIÓN 1: ACCIONES PRINCIPALES (Diario) --- #}
    <section class="mb-5">
        <h2 class="h5 border-bottom pb-2 mb-4 text-secondary"><i class="fa-solid fa-book-open me-2"></i> Mi Diario</h2>

        <div class="row g-4">
            {# Tarjeta: Nueva Entrada #}
            <div class="col-md-6">
                <div class="card border-0 shadow-sm rounded-4 h-100">
                    <div class="card-body p-4 d-flex align-items-center">
                        <div class="bg-primary bg-opacity-10 p-3 rounded-circle me-4 text-primary d-flex align-items-center justify-content-center" style="width: 60px; height: 60px;">
                            <i class="fa-solid fa-pen-fancy fs-3"></i>
                        </div>
                        <div class="flex-grow-1">
                            <h3 class="h5 fw-bold mb-1">Escribir Entrada de Hoy</h3>
                            <p class="text-muted small mb-3">Registra cómo te sientes y qué has hecho.</p>
                            <a href="{{ path('app_entry_new') }}" class="btn btn-primary rounded-pill px-4 stretched-link" aria-label="Escribir una nueva entrada en el diario">Empezar ahora</a>
                        </div>
                    </div>
                </div>
            </div>

            {# Tarjeta: Ver Historial #}
            <div class="col-md-6">
                <div class="card border-0 shadow-sm rounded-4 h-100">
                    <div class="card-body p-4 d-flex align-items-center">
                        <div class="bg-success bg-opacity-10 p-3 rounded-circle me-4 text-success d-flex align-items-center justify-content-center" style="width: 60px; height: 60px;">
                            <i class="fa-solid fa-clock-rotate-left fs-3"></i>
                        </div>
                        <div class="flex-grow-1">
                            <h3 class="h5 fw-bold mb-1">Ver mi Historial</h3>
                            <p class="text-muted small mb-3">Revisa tus entradas anteriores y reflexiona.</p>
                            <a href="{{ path('app_entry_index') }}" class="btn btn-outline-success rounded-pill px-4 stretched-link" aria-label="Ver el historial completo del diario">Abrir Timeline</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    {# --- SECCIÓN 2: GESTIÓN (Catálogos) --- #}
    <section>
        <h2 class="h5 border-bottom pb-2 mb-4 text-secondary"><i class="fa-solid fa-sliders me-2"></i> Configuración Personal</h2>

        <div class="row g-4 row-cols-1 row-cols-md-3">
            {# Tarjeta: Emociones #}
            <div class="col">
                <div class="card border-0 shadow-sm rounded-4 h-100 text-center py-4">
                    <div class="card-body">
                        <i class="fa-solid fa-face-smile fa-3x text-warning mb-3"></i>
                        <h3 class="h6 fw-bold mb-3">Emociones</h3>
                        <a href="{{ path('app_emotion_index') }}" class="btn btn-light text-primary rounded-pill px-4 stretched-link" aria-label="Gestionar catálogo de emociones">Gestionar</a>
                    </div>
                </div>
            </div>

            {# Tarjeta: Actividades #}
            <div class="col">
                <div class="card border-0 shadow-sm rounded-4 h-100 text-center py-4">
                    <div class="card-body">
                        <i class="fa-solid fa-person-running fa-3x text-info mb-3"></i>
                        <h3 class="h6 fw-bold mb-3">Actividades</h3>
                        <a href="{{ path('app_activity_index') }}" class="btn btn-light text-primary rounded-pill px-4 stretched-link" aria-label="Gestionar catálogo de actividades">Gestionar</a>
                    </div>
                </div>
            </div>

            {# Tarjeta: Etiquetas #}
            <div class="col">
                <div class="card border-0 shadow-sm rounded-4 h-100 text-center py-4">
                    <div class="card-body">
                        <i class="fa-solid fa-tags fa-3x text-secondary mb-3"></i>
                        <h3 class="h6 fw-bold mb-3">Etiquetas</h3>
                        <a href="{{ path('app_tag_index') }}" class="btn btn-light text-primary rounded-pill px-4 stretched-link" aria-label="Gestionar catálogo de etiquetas">Gestionar</a>
                    </div>
                </div>
            </div>
        </div>
    </section>

</div>
{% endblock %}
