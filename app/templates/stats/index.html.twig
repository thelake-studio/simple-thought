{% extends 'base.html.twig' %}

{% block title %}Mis Estadísticas{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">📊 Análisis Personal</h1>
        <p class="text-muted mb-0">Comprende tus emociones y hábitos</p>
    </div>

    <div class="row">
        {# Tarjeta 1: Evolución Semanal #}
        <div class="col-md-8 mb-4">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-header bg-white py-3 d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">
                        <i class="fa-solid fa-chart-line text-primary me-2"></i> Evolución del Ánimo (Esta Semana)
                    </h5>
                </div>
                <div class="card-body">
                    <canvas
                        data-controller="chart"
                        data-chart-datos-value="{{ moodChartData|json_encode }}"
                    ></canvas>
                </div>
            </div>
        </div>

        {# Espacio para futuras tarjetas (Ej: Distribución de Emociones) #}
        <div class="col-md-4 mb-4">
            <div class="card shadow-sm border-0 h-100 bg-light d-flex align-items-center justify-content-center">
                <div class="text-center text-muted">
                    <i class="fa-solid fa-chart-pie fa-3x mb-3 text-secondary opacity-50"></i>
                    <p class="mb-0">Próximamente:<br>Distribución Mensual</p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
