{% extends 'base.html.twig' %}

{% block title %}Editar Perfil - Simple Thought{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-white py-3">
                    <h4 class="mb-0"><i class="fa-solid fa-pen-to-square text-primary me-2"></i> Editar Mis Datos</h4>
                </div>
                <div class="card-body p-4">
                    {{ include('profile/_form.html.twig', {'button_label': 'Actualizar Perfil'}) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
