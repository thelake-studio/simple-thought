{% extends 'base.html.twig' %}

{% block title %}Editar Perfil - Simple Thought{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-10 col-xl-8">

            {# --- Cabecera --- #}
            <header class="mb-4">
                <a href="{{ path('app_profile_index') }}" class="text-decoration-none text-secondary small mb-3 d-inline-block" aria-label="Volver a mi perfil">
                    <i class="fa-solid fa-arrow-left me-2"></i>Volver a mi perfil
                </a>
                <h1 class="h3 fw-bold text-primary mb-1">
                    <i class="fa-solid fa-pen-to-square me-2"></i>Editar Mis Datos
                </h1>
                <p class="text-muted small mb-0">Actualiza tu nombre visible o tu correo electrónico asociado.</p>
            </header>

            {# --- Formulario Principal --- #}
            {{ include('profile/_form.html.twig', {'button_label': 'Actualizar Perfil'}) }}

        </div>
    </div>
</div>
{% endblock %}
