<form method="post" action="{{ path('app_profile_delete') }}" onsubmit="return confirm('¿Estás absolutamente seguro de que quieres borrar tu cuenta? Perderás todos tus registros para siempre.');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete_account' ~ user.id) }}">
    <button type="submit" class="btn btn-outline-danger btn-sm">
        <i class="fa-solid fa-trash-can"></i> Eliminar Cuenta
    </button>
</form>
