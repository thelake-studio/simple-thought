{% extends 'base.html.twig' %}

{% block title %}Mis Emociones - Simple Thought{% endblock %}

{% block body %}
<div class="container-fluid py-4">

    {# --- Cabecera de la Vista --- #}
    <header class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-5 gap-3">
        <div>
            <h1 class="h3 fw-bold text-primary mb-1">
                <i class="fa-solid fa-heart-pulse me-2"></i>Mis Emociones
            </h1>
            <p class="text-muted small mb-0">Define cómo te sientes para registrarlo en tu diario.</p>
        </div>
        <a href="{{ path('app_emotion_new') }}" class="btn btn-primary rounded-pill px-4 shadow-sm" aria-label="Crear una nueva emoción">
            <i class="fa-solid fa-plus me-2"></i>Nueva Emoción
        </a>
    </header>

    {# --- Contenido Principal --- #}
    <section>
        {% if emotions is empty %}
            {# --- Estado Vacío (Empty State) --- #}
            <div class="card border-0 shadow-sm rounded-4 text-center py-5">
                <div class="card-body">
                    <i class="fa-solid fa-face-smile fa-3x text-secondary mb-3"></i>
                    <h2 class="h5 fw-bold text-dark">¡Expresa lo que sientes!</h2>
                    <p class="text-muted mb-4">Aún no has registrado ninguna emoción.<br>Empieza definiendo tus estados de ánimo habituales (Feliz, Triste, Ansioso...).</p>
                    <a href="{{ path('app_emotion_new') }}" class="btn btn-primary rounded-pill px-4" aria-label="Crear mi primera emoción">
                        Crear mi primera emoción
                    </a>
                </div>
            </div>
        {% else %}
            {# --- Cuadrícula de Emociones --- #}
            <div class="row row-cols-1 row-cols-md-2 row-cols-xl-3 g-4">
                {% for emotion in emotions %}
                    <div class="col">
                        <div class="card h-100 border-0 shadow-sm rounded-4">

                            {# Cabecera de Color Dinámico #}
                            <div class="card-header border-0 rounded-top-4 py-3 text-white" style="background-color: {{ emotion.color|default('#6c757d') }};">
                                <div class="d-flex justify-content-between align-items-center">
                                    <h3 class="h6 fw-bold mb-0 text-truncate">
                                        {% if emotion.icon %}
                                            <i class="{{ emotion.icon }} me-2"></i>
                                        {% endif %}
                                        {{ emotion.name }}
                                    </h3>
                                    <span class="badge bg-white bg-opacity-25 text-white border border-white rounded-pill px-3 shadow-sm" title="Intensidad de la emoción">
                                        {{ emotion.value }}/10
                                    </span>
                                </div>
                            </div>

                            <div class="card-body">
                                <p class="card-text text-muted small mb-0">
                                    Identificador visual y configuración para tus registros diarios.
                                </p>
                            </div>

                            <div class="card-footer bg-white border-0 rounded-bottom-4 text-end pb-3 pe-3 pt-0">
                                <div class="d-flex justify-content-end gap-2">
                                    <a href="{{ path('app_emotion_show', {'id': emotion.id}) }}" class="btn btn-sm btn-light text-primary rounded-circle" aria-label="Ver detalle de {{ emotion.name }}" title="Ver detalle">
                                        <i class="fa-solid fa-eye"></i>
                                    </a>
                                    <a href="{{ path('app_emotion_edit', {'id': emotion.id}) }}" class="btn btn-sm btn-light text-secondary rounded-circle" aria-label="Editar {{ emotion.name }}" title="Editar">
                                        <i class="fa-solid fa-pen"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </section>
</div>
{% endblock %}
