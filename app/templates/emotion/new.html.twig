{% extends 'base.html.twig' %}

{% block title %}Nueva Emoción - Simple Thought{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-10 col-xl-8">

            {# --- Cabecera --- #}
            <header class="mb-4">
                <a href="{{ path('app_emotion_index') }}" class="text-decoration-none text-secondary small mb-3 d-inline-block" aria-label="Volver a mis emociones">
                    <i class="fa-solid fa-arrow-left me-2"></i>Volver al listado
                </a>
                <h1 class="h3 fw-bold text-primary mb-1">
                    <i class="fa-solid fa-plus-circle me-2"></i>Registrar Nueva Emoción
                </h1>
                <p class="text-muted small mb-0">Define un nuevo estado de ánimo para usar en tu diario.</p>
            </header>

            {# --- Formulario --- #}
            {% include 'emotion/_form.html.twig' with {'button_label': 'Crear Emoción'} %}

        </div>
    </div>
</div>
{% endblock %}
