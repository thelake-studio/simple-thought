{% extends 'base.html.twig' %}

{% block title %}Editar Actividad{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h1 class="h3 mb-4 text-success"><i class="fas fa-edit"></i> Editar: {{ activity.name }}</h1>

            {% include 'activity/_form.html.twig' with {'button_label': 'Actualizar'} %}

            <div class="mt-3 text-end">
                {# Comentado hasta que creemos el delete form #}
                {# {{ include('activity/_delete_form.html.twig') }} #}
            </div>
        </div>
    </div>
</div>
{% endblock %}
