{% extends 'base.html.twig' %}

{% block title %}Mis Actividades{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 text-success"><i class="fas fa-running"></i> Mis Actividades</h1>
            <p class="text-muted small mb-0">Gestiona tus hábitos, deportes y tareas.</p>
        </div>
        <a href="{{ path('app_activity_new') }}" class="btn btn-success text-white shadow-sm">
            <i class="fas fa-plus"></i> Nueva Actividad
        </a>
    </div>

    {% if activities is empty %}
        <div class="alert alert-light border text-center shadow-sm py-5">
            <div class="text-success mb-3">
                <i class="fas fa-dumbbell fa-3x"></i>
            </div>
            <h4 class="alert-heading">¡A mover el esqueleto!</h4>
            <p class="mb-0">Aún no has registrado ninguna actividad.</p>
            <p class="text-muted">Define qué haces en tu día a día (Trabajo, Gym, Leer...) para poder trackearlo luego.</p>
            <a href="{{ path('app_activity_new') }}" class="btn btn-outline-success mt-3">Crear mi primera actividad</a>
        </div>
    {% else %}
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            {% for activity in activities %}
                <div class="col">
                    <div class="card h-100 shadow-sm border-0 hover-effect">
                        {# Cabecera con color dinámico #}
                        <div class="card-header border-0 d-flex justify-content-between align-items-center text-white"
                             style="background-color: {{ activity.color|default('#198754') }};">
                            <h5 class="card-title mb-0 text-truncate">
                                {% if activity.icon %}
                                    <i class="{{ activity.icon }} me-1"></i>
                                {% endif %}
                                {{ activity.name }}
                            </h5>
                        </div>

                        <div class="card-body">
                            {% if activity.category %}
                                <span class="badge bg-light text-secondary border mb-2">
                                    {{ activity.category }}
                                </span>
                            {% endif %}

                            <p class="card-text text-muted small">
                                {% if activity.description %}
                                    {{ activity.description|u.truncate(80, '...') }}
                                {% else %}
                                    <span class="fst-italic">Sin descripción.</span>
                                {% endif %}
                            </p>
                        </div>

                        <div class="card-footer bg-white border-top-0 text-end">
                            <a href="{{ path('app_activity_show', {'id': activity.id}) }}" class="btn btn-sm btn-outline-primary" title="Ver detalle">
                                <i class="fas fa-eye"></i>
                            </a>
                            <a href="{{ path('app_activity_edit', {'id': activity.id}) }}" class="btn btn-sm btn-outline-secondary" title="Editar">
                                <i class="fas fa-edit"></i>
                            </a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
</div>

<style>
    .hover-effect { transition: transform 0.2s; }
    .hover-effect:hover { transform: translateY(-5px); }
</style>
{% endblock %}
