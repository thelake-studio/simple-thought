{% extends 'base.html.twig' %}

{% block title %}Nueva Etiqueta - Simple Thought{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-10 col-xl-8">

            {# --- Cabecera --- #}
            <header class="mb-4">
                <a href="{{ path('app_tag_index') }}" class="text-decoration-none text-secondary small mb-3 d-inline-block" aria-label="Volver a mis etiquetas">
                    <i class="fa-solid fa-arrow-left me-2"></i>Volver al listado
                </a>
                <h1 class="h3 fw-bold text-primary mb-1">
                    <i class="fa-solid fa-plus-circle me-2"></i>Registrar Nueva Etiqueta
                </h1>
                <p class="text-muted small mb-0">Crea una nueva etiqueta para clasificar tus entradas.</p>
            </header>

            {# --- Formulario --- #}
            {% include 'tag/_form.html.twig' with {'button_label': 'Crear Etiqueta'} %}

        </div>
    </div>
</div>
{% endblock %}
