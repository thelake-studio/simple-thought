{% extends 'base.html.twig' %}

{% block title %}Mis Etiquetas - Simple Thought{% endblock %}

{% block body %}
<div class="container-fluid py-4">

    {# --- Cabecera de la Vista --- #}
    <header class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-5 gap-3">
        <div>
            <h1 class="h3 fw-bold text-primary mb-1">
                <i class="fa-solid fa-tags me-2"></i>Mis Etiquetas
            </h1>
            <p class="text-muted small mb-0">Organiza tus pensamientos en diferentes categorías.</p>
        </div>
        <a href="{{ path('app_tag_new') }}" class="btn btn-primary rounded-pill px-4 shadow-sm" aria-label="Crear una nueva etiqueta">
            <i class="fa-solid fa-plus me-2"></i>Nueva Etiqueta
        </a>
    </header>

    {# --- Contenido Principal --- #}
    <section>
        {% if tags is empty %}
            {# --- Estado Vacío (Empty State) --- #}
            <div class="card border-0 shadow-sm rounded-4 text-center py-5">
                <div class="card-body">
                    <i class="fa-solid fa-tags fa-3x text-secondary mb-3"></i>
                    <h2 class="h5 fw-bold text-dark">Sin etiquetas</h2>
                    <p class="text-muted mb-4">Aún no has creado ninguna etiqueta para organizar tu diario.</p>
                    <a href="{{ path('app_tag_new') }}" class="btn btn-primary rounded-pill px-4" aria-label="Crear mi primera etiqueta">
                        Crear mi primera etiqueta
                    </a>
                </div>
            </div>
        {% else %}
            {# --- Cuadrícula de Etiquetas --- #}
            {# Usamos 4 columnas en PC porque las etiquetas ocupan muy poco espacio #}
            <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-xl-4 g-4">
                {% for tag in tags %}
                    <div class="col">
                        <div class="card h-100 border-0 shadow-sm rounded-4">
                            <div class="card-body d-flex align-items-center p-4">

                                {# Círculo de color identificativo #}
                                <span class="rounded-circle d-inline-block me-3 shadow-sm border" style="width: 24px; height: 24px; background-color: {{ tag.color|default('#6c757d') }};"></span>

                                {# Nombre de la etiqueta #}
                                <h2 class="h6 mb-0 fw-bold text-truncate flex-grow-1">
                                    {{ tag.name }}
                                </h2>
                            </div>

                            {# Botones de acción minimalistas #}
                            <div class="card-footer bg-white border-0 rounded-bottom-4 text-end pb-3 pe-3 pt-0">
                                <div class="d-flex justify-content-end gap-2">
                                    <a href="{{ path('app_tag_show', {'id': tag.id}) }}" class="btn btn-sm btn-light text-primary rounded-circle" aria-label="Ver detalle de {{ tag.name }}" title="Ver detalle">
                                        <i class="fa-solid fa-eye"></i>
                                    </a>
                                    <a href="{{ path('app_tag_edit', {'id': tag.id}) }}" class="btn btn-sm btn-light text-secondary rounded-circle" aria-label="Editar {{ tag.name }}" title="Editar">
                                        <i class="fa-solid fa-pen"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </section>
</div>
{% endblock %}
